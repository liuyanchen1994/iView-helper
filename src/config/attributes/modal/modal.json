{
  "Modal/value": {
    "description": "对话框是否显示，可使用 v-model 双向绑定数据",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/title": {
    "description": "对话框标题，如果使用 slot 自定义了页头，则 title 无效",
    "optionTyep": "String",
    "defaultValue": "-"
  },"Modal/closable": {
    "description": "是否显示右上角的关闭按钮，关闭后 Esc 按键也将关闭",
    "optionTyep": "Boolean",
    "defaultValue": "true"
  },"Modal/mask-closable": {
    "description": "是否允许点击遮罩层关闭",
    "optionTyep": "Boolean",
    "defaultValue": "true"
  },"Modal/loading": {
    "description": "点击确定按钮时，确定按钮是否显示 loading 状态，开启则需手动设置value来关闭对话框",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/scrollable": {
    "description": "页面是否可以滚动",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/fullscreen": {
    "description": "是否全屏显示",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/draggable": {
    "description": "是否可以拖拽移动",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/mask": {
    "description": "是否显示遮罩层，开启 draggable 时，强制不显示",
    "optionTyep": "Boolean",
    "defaultValue": "true"
  },"Modal/ok-text": {
    "description": "确定按钮文字",
    "optionTyep": "String",
    "defaultValue": "确定"
  },"Modal/cancel-text": {
    "description": "取消按钮文字",
    "optionTyep": "String",
    "defaultValue": "取消"
  },"Modal/width": {
    "description": "对话框宽度，对话框的宽度是响应式的，当屏幕尺寸小于 768px 时，宽度会变为自动auto。当其值不大于 100 时以百分比显示，大于 100 时为像素",
    "optionTyep": "Number | String",
    "defaultValue": "520"
  },"Modal/footer-hide": {
    "description": "不显示底部",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Modal/styles": {
    "description": "设置浮层样式，调整浮层位置等，该属性设置的是.ivu-modal的样式",
    "optionTyep": "Object",
    "defaultValue": "-"
  },"Modal/class-name": {
    "description": "设置对话框容器.ivu-modal-wrap的类名，可辅助实现垂直居中等自定义效果",
    "optionTyep": "String",
    "defaultValue": "-"
  },"Modal/z-index": {
    "description": "层级",
    "optionTyep": "Number",
    "defaultValue": "1000"
  },"Modal/transition-names": {
    "description": "自定义显示动画，第一项是模态框，第二项是背景",
    "optionTyep": "Array",
    "defaultValue": "['ease', 'fade']"
  },"Modal/transfer": {
    "description": "是否将弹层放置于 body 内",
    "optionTyep": "Boolean",
    "defaultValue": "true"
  },"Modal/@on-ok": {
    "description": "点击确定的回调,返回值 无",
    "optionTyep": "Function"
  },"Modal/@on-cancel": {
    "description": "点击取消的回调,返回值 无",
    "optionTyep": "Function"
  },"Modal/@on-visible-change": {
    "description": "显示状态发生变化时触发,返回值 true / false",
    "optionTyep": "Function"
  },"Modal/header": {
    "description": "自定义页头",
    "optionTyep": "Slot"
  },"Modal/footer": {
    "description": "自定义页脚内容",
    "optionTyep": "Slot"
  },"Modal/close": {
    "description": "自定义右上角关闭内容",
    "optionTyep": "Slot"
  }
}
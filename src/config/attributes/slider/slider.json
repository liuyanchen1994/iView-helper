{
  "Slider/value": {
    "description": "滑块选定的值，可以使用 v-model 双向绑定数据。普通模式下，数据格式为数字，在双滑块模式下，数据格式为长度是2的数组，且每项都为数字",
    "optionTyep": "Number | Array",
    "defaultValue": "0"
  },
  "Slider/min": {
    "description": "最小值",
    "optionTyep": "Number",
    "defaultValue": "0"
  },
  "Slider/max": {
    "description": "最大值",
    "optionTyep": "Number",
    "defaultValue": "100"
  },
  "Slider/step": {
    "description": "步长，取值建议能被（max - min）整除",
    "optionTyep": "Number",
    "defaultValue": "1"
  },
  "Slider/disabled": {
    "description": "是否禁用滑块",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },
  "Slider/range": {
    "description": "是否开启双滑块模式",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },
  "Slider/show-input": {
    "description": "是否显示数字输入框，仅在单滑块模式下有效",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },
  "Slider/show-stops": {
    "description": "是否显示间断点，建议在 step 不密集时使用",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },
  "Slider/show-tip": {
    "options": [
      "hover",
      "always",
      "never"
    ],
    "description": "提示的显示控制，可选值为 hover（悬停，默认）、always（总是可见）、never（不可见）",
    "optionTyep": "String",
    "defaultValue": "hover"
  },
  "Slider/tip-format": {
    "description": "Slider 会把当前值传给 tip-format，并在 Tooltip 中显示 tip-format 的返回值，若为 null，则隐藏 Tooltip",
    "optionTyep": "Function",
    "defaultValue": "value"
  },
  "Slider/input-size": {
    "options": [
      "large",
      "small",
      "default"
    ],
    "description": "数字输入框的尺寸，仅在开启 show-input 时有效",
    "optionTyep": "String",
    "defaultValue": "default"
  },
  "Slider/active-change": {
    "description": "同 InputNumber 的 active-change",
    "optionTyep": "Boolean",
    "defaultValue": "true"
  },
  "Slider/marks": {
    "description": "标记， key 的类型必须为 number 且取值在闭区间 [min, max] 内，每个标记可以单独设置样式",
    "optionTyep": "Object",
    "defaultValue": "-"
  },
  "Slider/@on-change": {
    "description": "在松开滑动时触发，返回当前的选值，在滑动过程中不会触发,返回value",
    "optionTyep": "Function"
  },
  "Slider/@on-input": {
    "description": "滑动条数据变化时触发，返回当前的选值，在滑动过程中实时触发,返回value",
    "optionTyep": "Function"
  }
}
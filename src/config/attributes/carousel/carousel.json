{
  "Carousel/value": {
    "description": "幻灯片的索引，从 0 开始，可以使用 v-model 双向绑定数据",
    "optionTyep": "Number",
    "defaultValue": "0"
  },"Carousel/height": {
    "description": "走马灯的高度，可填 auto 或具体高度数值，单位 px",
    "optionTyep": "String | Number",
    "defaultValue": "auto"
  },"Carousel/loop": {
    "description": "是否开启循环",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Carousel/autoplay": {
    "description": "是否自动切换",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Carousel/autoplay-speed": {
    "description": "自动切换的时间间隔，单位为毫秒",
    "optionTyep": "Number",
    "defaultValue": "2000"
  },"Carousel/dots": {
    "options":["inside","outside","none"],
    "description": "指示器的位置，可选值为 inside （内部），outside（外部），none（不显示）",
    "optionTyep": "String",
    "defaultValue": "inside"
  },"Carousel/radius-dot": {
    "description": "是否显示圆形指示器",
    "optionTyep": "Boolean",
    "defaultValue": "false"
  },"Carousel/trigger": {
    "options":["click","hover"],
    "description": "指示器的触发方式，可选值为 click（点击），hover（悬停）",
    "optionTyep": "String",
    "defaultValue": "click"
  },"Carousel/arrow": {
    "options":["hover","always","never"],
    "description": "切换箭头的显示时机，可选值为 hover（悬停），always（一直显示），never（不显示）",
    "optionTyep": "String",
    "defaultValue": "hover"
  },"Carousel/easing": {
    "description": "动画效果",
    "optionTyep": "String",
    "defaultValue": "ease"
  },"Carousel/@on-change": {
    "description": "幻灯片切换时触发，目前激活的幻灯片的索引，原幻灯片的索引,oldValue, value",
    "optionTyep": "function",
    "defaultValue": "-"
  }
}